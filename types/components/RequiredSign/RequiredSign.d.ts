/// <reference types="react" />
import { ComponentSize } from "../../definitions";
export interface RequiredSignProps {
    size?: ComponentSize;
    className?: string;
}
export declare function RequiredSign({ size, className }: RequiredSignProps): JSX.Element;
