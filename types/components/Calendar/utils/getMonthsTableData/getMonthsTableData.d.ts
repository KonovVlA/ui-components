/**
 * Возвращает массив (ряд) из массивов (колонка) месяцев года выбранного периода.
 * Используется в отрисовке таблицы выбора месяца.
 */
export declare function getMonthsTableData(span: Date, rows?: number, cols?: number): Date[][];
